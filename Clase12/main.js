//Array

//Definir array
//const list = ['manzana', 'pera', 'uva', 'piña'];
//Lista variada con strings, booleanos, otras listas
const randomList = ['pera', 32, false, ['uvas']];
//Listas vacias
const empty = [];

//Acceder elementos de una array
const fruit = list[2];
console.log(list.length);
console.log(fruit);

//Agregar elementos a la lista
//list.push('naranja');
console.log (list);

//Eliminar elementos
//list.splice(2, 1);
console.log(list);

//Loops
for(let index = 0; i <list.length; i++){
    console.log(list[i]);
}
